📌 Что такое half-duplex и full-duplex
🔁 Half-duplex ("полудуплекс")
Устройство может либо передавать, либо принимать данные, но не одновременно.
Пример из жизни: рация — один говорит, другой слушает. Если оба начнут одновременно → мешанина.
В сетях: в старом Ethernet через хабы — возможны коллизии (два устройства одновременно отправили → столкновение).
---
🔁 Full-duplex ("полный дуплекс")
Можно одновременно передавать и принимать данные.
Пример из жизни: телефонный разговор — оба могут говорить и слушать одновременно.
В сетях: современные коммутаторы и сетевые карты всегда работают в full-duplex.
Коллизий нет, потому что каналы передачи и приёма разделены.
---
📌 ARP (Address Resolution Protocol)
ARP = протокол разрешения адресов.
 Он нужен, чтобы узнать: какой MAC-адрес соответствует конкретному IP-адресу в локальной сети.

arp -a
-----
📦 Что такое кадр (frame)
Кадр — это единица данных на канальном уровне (Layer 2, Data Link Layer) модели OSI.
Когда твой компьютер хочет передать информацию по сети, данные проходят через уровни:
На прикладном уровне — это может быть HTTP-запрос.
На транспортном уровне (TCP/UDP) — это сегменты.
На сетевом уровне (IP) — это пакеты.
На канальном уровне (Ethernet, Wi-Fi) — эти пакеты упаковываются в кадры.
На физическом уровне кадры передаются уже в виде сигналов (электрических, оптических или радиоволн).

📡 Что такое канальная среда или один широковещательный домен
Канальная среда передачи данных — это физическая среда, по которой реально передаются биты на канальном уровне (L2) модели OSI.
Иными словами, это провод или беспроводной канал, по которому кадры (frames) перемещаются между устройствами в пределах одной локальной сети.
----
В современных коммутаторах есть два типа архитектуры:

Input queuing (очередь на входе):
Кадры выстраиваются в очередь во входном буфере.
Минус — возможен эффект HOL blocking (Head-of-Line blocking): 
если первый пакет в очереди ждёт (например, его нельзя отправить, т.к. выходной порт занят), 
то все следующие тоже ждут.

Output queuing (очередь на выходе):
Кадры сразу проходят через коммутатор и ждут отправки в выходном буфере.
Минус — нужна очень быстрая внутренняя шина (чтобы коммутатор мог сразу пересылать пакеты в правильный выходной порт).
Combined / shared memory (комбинированная архитектура):
Используются и входные, и выходные буферы, часто с общей динамической памятью.
Это баланс: меньше потерь и меньше эффектов HOL blocking.